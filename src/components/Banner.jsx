import React from "react";

const Banner = () => {
    return (
        <div className="banner">
            <h1>Manage Tasks</h1>
            <button>Add New Task</button>
        </div>
    );
};

export default Banner;
